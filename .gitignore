/.lake
/.venv

# LaTeX auxiliary files (keep the PDF)
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/missfont.log

# Python
__pycache__/
*.pyc
*.pyo

# Blueprint generated files
blueprint/src/web/
blueprint/src/print/
blueprint/*.pdf

# Manim output (videos, images, TeX cache)
media/

# Old Pillow-based GIFs (to be removed after migration)
chebyshev_gifs/

# Extracted frames (optional - comment out if you want to commit these)
chebyshev_frames/

# Manim videos for GitHub Pages (DO commit these for web deployment)
!chebyshev_videos/